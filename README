A debian jessie distribution with openbox, terminator, fish shell and neovim.With rdp access.

docker run -d -P -p 3370:3389 \
                 -v /home/docker/repo:/home/dev/repo \
                 -v /home/docker/container/test/home:/home/dev \
                 --name x11-base \
debitux/x11-base:latest
